<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.JournalApp">

        <!-- MainActivity: The entry point of the app -->
        <activity
            android:name=".activities.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- LoginActivity: Handles user login -->
        <activity
            android:name=".activities.LoginActivity"
            android:exported="false" />

        <!-- RegisterActivity: Handles user registration -->
        <activity
            android:name=".activities.RegisterActivity"
            android:exported="true" />

        <!-- AddEditEntryActivity: Handles adding or editing journal entries -->
        <activity
            android:name=".activities.AddEditEntryActivity"
            android:exported="false" />

    </application>

</manifest>
